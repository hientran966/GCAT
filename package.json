{
  "name": "app",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run dev --prefix web\" \"npm start --prefix server\" \"npm run ngrok\"",
    "ngrok": "wait-on http://localhost:3000 && ngrok http 3000"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "wait-on": "^7.2.0"
  }
}